spring:
    datasource:
        driverClassName: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/kotlin-mysql?useTimezone=true&serverTimezone=UTC
        username: root
        password: root
    flyway:
        url: jdbc:mysql://localhost:3306/kotlin-mysql?useTimezone=true&serverTimezone=UTC
        user: root
        password: root
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL8Dialect
        show-sql: false